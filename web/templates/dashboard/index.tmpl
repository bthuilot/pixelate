<!DOCTYPE html>
<html lang="en">
<head>
    {{template "head.tmpl"}}
    <script defer src="/static/js/dashboard.js"></script>
    <title>Pi Matrix</title>
</head>
<body>
<div class="container">
    <div id="loadingIcon" class="d-flex justify-content-center">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
    <a id="serviceConfigLink" href="#" style="visibility: hidden">Go to service config</a>
    <div id="serviceForm" style="visibility: hidden">
        <form action="/service" method="POST">
            <select name="service" id='serviceSelector' class="form-select" aria-label="Service" re>
            </select>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        <button type="submit" class="btn btn-danger" onclick="stopService()">Stop current service</button>
    </div>
</div>
</body>
</html>